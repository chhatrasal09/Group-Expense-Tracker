<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.chhatrasal.groupexpensetracker">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".GPE"
        android:theme="@style/AppTheme">

        <activity android:name=".StartOrViewTrip">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".TripHistory" />
        <activity android:name=".AddMembers" />
        <activity android:name=".OfflineHome" />
        <activity android:name=".OnlineHome"/>
        <activity android:name=".Home" />
        <activity android:name=".OfflineRegistration" />
        <activity android:name=".OnlineRegistration" />
    </application>

</manifest>